{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capability set for the wipe desktop window.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
